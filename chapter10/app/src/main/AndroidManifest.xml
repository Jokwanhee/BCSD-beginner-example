<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
<!--    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>-->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Chapter10"
        android:requestLegacyExternalStorage="true"
        tools:targetApi="31">

        <activity
            android:name=".practice.PracticeActivity"
            android:exported="true" />
        <service android:name=".practice.service.PracticeService"
            android:enabled="true"
            android:exported="false"/>
        <receiver android:name=".practice.receiver.PracticeReceiver"
            android:enabled="true"
            android:exported="false"/>

        <service
            android:name=".applyService.service.BasicService"
            android:enabled="true"
            android:exported="false" />
        <service
            android:name=".foregroundService.service.ForegroundService"
            android:enabled="true"
            android:exported="false" />

        <activity
            android:name=".foregroundService.ForegroundServiceActivity"
            android:exported="true" />
        <activity
            android:name=".applyService.BasicServiceActivity"
            android:exported="true" />
        <activity
            android:name=".localResourceMusic.LocalResourceMusicActivity"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>